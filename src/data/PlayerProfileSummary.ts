namespace we {
  export namespace data {
    export class PlayerProfileSummary {
      public maxwin: number; // maximum win amount
      public winningstreak: number; // maximum winning streak
      // following, follower, etc
    }
  }
}
