namespace we {
  export namespace data {
    export class PlayerBetResult {
      public success: boolean; // indicated if success
      public error: number; // error detail code if not success
    }
  }
}
